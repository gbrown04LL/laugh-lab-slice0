{
  "framework": "nextjs",
  "buildCommand": "prisma migrate deploy && prisma generate && next build",
  "installCommand": "npm install",
  "functions": {
    "src/app/api/jobs/[job_id]/run/route.ts": {
      "maxDuration": 300
    }
  }
}
